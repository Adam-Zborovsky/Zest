# M5 review — Ingredient Constellation and drift catalog

Independent review session, 2026-09-12, on commits d628bad..02a1051 (catalog storage, resumable A–Z sync, constellation graph/layout/UI, graph-first home). The reviewer read the project contracts (AGENTS.md, ROADMAP M5, PRODUCT.md constellation rules, DESIGN.md, GATEWAY.md, DATA.md), audited the full diff, and ran verification itself: `flutter analyze` clean, 178 tests passing.

## Verdict: BLOCK (initially)

The engineering core — transactional storage, run-token sync cancellation, deterministic graph and layout, restart resumability — was judged sound, with spot-verified web-asset hashes matching the pinned drift release. Two major findings blocked approval; all findings were then fixed in one commit and re-verified.

## Findings and resolution

| # | Severity | Finding | Resolution |
| --- | --- | --- | --- |
| 1 | major | Home copy claimed every ingredient gets a place; search counts used the bounded node list as denominator, hiding the top-40 bound | Intro reworded to "most-used ingredients"; `totalIdentityCount` added to the graph; count lines now read "Showing the top 40 of N ingredients by prevalence"; list card carries the same disclosure |
| 2 | major | Finished-card "Check for updates" button called `start()` over zero pending letters — a permanent no-op presented as an action | Button removed; the finished card is informational; genuine re-sync left as a separate scope decision |
| 3 | minor | `ConstellationPaintData.dispose()` dead code; label TextPainters and the settle CurvedAnimation leaked per rebuild | Previous paint data and the curve are now disposed at all teardown sites |
| 4 | minor | Painter doc claimed precomputed Paints while allocating them per frame | All Paints hoisted into the prepared paint data; `paint()` allocates nothing |
| 5 | minor | Sub-48px targets: list connection rows ~45px; edge corridor 32px | Rows raised to the 48px minimum; the edge corridor recorded in code as a deliberate exception with its three non-canvas equivalents |
| 6 | minor | Store errors from start()/resume() preamble escaped as unhandled async exceptions | Preamble wrapped into the same resumable paused-error handling |
| 7 | minor | Full-catalog JSON re-decode plus graph rebuild on every applied letter (~2s cadence) — the main 60fps risk | Graph invalidation coalesced during active sync; coverage stays live; graph refreshes when the run settles |
| 8 | note | Cross-letter shared-recipe behavior traced, no engine-reachable defect | Invariant documented on the `firstLetter` column |
| 9 | note | Reduced-motion test covered only `disableAnimations` | Test loops over both DESIGN.md flags |
| 10 | note | `completenessGuidance` doc overstated its placement | Doc comment reconciled to the finished coverage surface |
| 11 | note | Cooldowns without `retryAt` never ticked; resume stayed disabled | Fallback deadline synthesized from remaining seconds |
| 12 | note | Stale comments and a dead ignore directive | Cleaned |
| 13 | note | No home 320px/text-scaling readability test | Added at 1× and 2× under reduced motion |

## Re-verification

After the fixes (commit `6dc7d00`), the main agent re-ran from `frontend/`: `flutter analyze` — no issues; `flutter test` — **186 passed**; `flutter build web --release` — succeeds. No findings were waived.

## Not verified by either session

The on-device 60 fps profile run (Adam's pending acceptance step — see finding 7 for the code-level risk already mitigated), live gateway/paid-key traffic, the browser WASM runtime path, physical screen-reader sessions, and native device runs.
